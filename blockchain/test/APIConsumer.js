const { expect } = require('chai')
const APIConsumerContract = artifacts.require('./APIConsumerContract')
const IERC20 = artifacts.require('./IERC20')

contract('APIConsumer Contract', async () => {
    
    before( async() => {

    })
})

